# These are public (anon) Supabase credentials required for frontend access.
# Even though these are considered "safe" for browser use, your actual .env file should never be committed.

VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-public-anon-key